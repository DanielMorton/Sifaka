use sprs::CsMatI;

fn top_n_recommendations(user_item: &CsMatI<N, I>,
                         users: &Vec<I>,
                         neighbors: &u32,
                         corr: Correlation) -> Vec<I> {

}